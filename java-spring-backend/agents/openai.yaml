interface:
  display_name: "Java Spring Backend Development"
  short_description: "Implement Java/Spring/MyBatis/Rabbit-SQL/PostgreSQL backend changes using centralized team standards and clear responsibility boundaries"
  default_prompt: "Use $java-spring-backend to implement the current backend task according to the target project's existing design. Build context, determine responsibilities, use references/routing.md to load only the required standards, and make the minimum necessary code change. Do not duplicate formal code review or executable verification; use $backend-code-review and $review-and-test for those completion stages."
policy:
  allow_implicit_invocation: true
