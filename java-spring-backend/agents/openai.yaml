interface:
  display_name: "Java Spring Backend Development"
  short_description: "Develop Java/Spring backends according to team standards, loading module structure, layering, API, MyBatis/Rabbit-SQL/SQL, transaction, concurrency, and testing rules on demand"
  default_prompt: "Use $java-spring-backend to complete the current backend task according to the target project's existing design. First determine responsibilities and the persistence framework, load only the required references, make only necessary changes, and run the relevant validation."
policy:
  allow_implicit_invocation: true
